[phases.setup]
nixPkgs = ['php81', 'php81Extensions.pdo_mysql', 'php81Extensions.zip', 'php81Extensions.gd']

[phases.build]
cmds = ['composer install --no-dev', 'php artisan config:cache', 'php artisan route:cache']

[start]
cmd = 'php artisan serve --host 0.0.0.0 --port $PORT'